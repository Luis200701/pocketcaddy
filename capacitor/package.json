{
  "name": "pocketcaddy",
  "version": "0.46.0",
  "private": true,
  "description": "Pocket Caddy - iOS-Huelle (Capacitor) um die Web-App",
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "sync": "npm run build:www && cap sync ios",
    "ios": "npm run sync && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.2",
    "@capacitor/ios": "^8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.2"
  }
}
